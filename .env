# Domain
# This would be set to the production domain with an env var on deployment
# used by Traefik to transmit traffic and aqcuire TLS certificates
DOMAIN=seboadx.com 
# To test the local Traefik config
# DOMAIN=localhost.tiangolo.com

# Environment: local, staging, production
ENVIRONMENT=local

PROJECT_NAME="AIPicAPP"
STACK_NAME=ai-pic-app

# Backend
BACKEND_CORS_ORIGINS="http://localhost,https://localhost,http://localhost.tiangolo.com"
SECRET_KEY=kvhzQRtDpokErUm1ZLpx

# Emails
SMTP_HOST=
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=info@example.com
SMTP_TLS=True
SMTP_SSL=False
SMTP_PORT=587

# Postgres
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=app
POSTGRES_USER=ai_pic_admin
POSTGRES_PASSWORD=kvhzQRtDpokErUm1ZLpx

SENTRY_DSN=

# Configure these with your own Docker registry images
DOCKER_IMAGE_BACKEND=backend

# Snowflake
SNOWFLAKE_NODE_ID=0

# Redis (local dev)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Aliyun OSS (client direct upload; replace with real values for production)
OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
OSS_BUCKET=pickitchen-dev
OSS_ACCESS_KEY_ID=test_access_key_id
OSS_ACCESS_KEY_SECRET=test_access_key_secret
OSS_DIR_PREFIX=uploads
OSS_UPLOAD_EXPIRE_SECONDS=60

# Aliyun Emoji API
ALIYUN_EMOJI_MOCK=true

# RevenueCat webhook auth (set this in RevenueCat dashboard too)
REVENUECAT_WEBHOOK_SECRET=test_webhook_secret
